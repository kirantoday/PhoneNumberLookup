package com.oxiotest.phonenumberlookup;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the PhoneNumberLookup Spring Boot application.
 */
@SpringBootApplication
public class PhoneNumberLookupApplication {

    /**
     * Boots the application.
     *
     * @param args command-line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(PhoneNumberLookupApplication.class, args);
    }

}
